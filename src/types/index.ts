export interface BusinessConfig {
  name: string
  category: string
  template: string
  modules: string[]
  theme: string
}

export interface ContactForm {
  name: string
  email: string
  message: string
}